{
  "name": "json-to-csv",
  "version": "1.0.0",
  "description": "Convert json to csv",
  "main": "index.js",
  "scripts": {
    "dev": "browser-sync start -s src -f src",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Andreas Nilsson",
  "license": "MIT",
  "devDependencies": {
    "browser-sync": "^2.18.12"
  }
}
